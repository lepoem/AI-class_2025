첨부한 파일을 자신의 컴퓨터 저장받고,
저장경로를 확인한 후에 DT, RF, SVM, LR 분류를 각각 수행해보세요.


import pandas as pd

# 붓꽃 데이터 CSV 파일 읽기
file_path = "C:/저장경로/iris.csv"  # 본인이 iris.csv를 저장한 경로를 입력합니다.
df = pd.read_csv(file_path)

# 데이터프레임 확인
df.head()
df.columns()
